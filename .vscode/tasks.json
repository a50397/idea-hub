{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "mongo: up",
      "type": "shell",
      "command": "docker compose up -d --wait mongodb",
      "options": { "cwd": "${workspaceFolder}" },
      "presentation": { "reveal": "silent", "close": true },
      "problemMatcher": []
    }
  ]
}
